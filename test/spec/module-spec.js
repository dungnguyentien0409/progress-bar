"use strict";

describe('Testing the functionality, this is module', ()=>{
    it('create module successfully', () => {
        let module = MODULE;
        expect(module).not.toEqual(null);
    })
})